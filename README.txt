# 9U Draft Board (PWA) — Fixed & Enhanced
- Sticky table headers now work inside scroll containers (no overlay on first row).
- "Batch Grade" renamed to **Overall**.
- Added **Footwork** and **Baseball IQ** as separate attributes (in Score Player + Overall table).
- Widened number inputs in tables for readability.
- Players CSV now includes data from Overall, Pitcher, and Catcher sections.
- Rankings tables scroll correctly; headers no longer cover first candidate.

## Deploy to GitHub Pages
Place all files in the repository root and enable Pages (Deploy from a branch, root). Open in Safari → Add to Home Screen.
